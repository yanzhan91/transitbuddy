{
    "interactionModel": {
        "languageModel": {
            "invocationName": "transit buddy",
            "intents": [
                {
                    "name": "GetBusIntent",
                    "slots": [
                        {
                            "name": "preset_id",
                            "type": "AMAZON.NUMBER"
                        }
                    ],
                    "samples": [
                        "preset {preset_id}",
                        "get preset {preset_id}",
                        "get my bus",
                        "get my bus at preset {preset_id}",
                        "get my bus for preset {preset_id}",
                        "get my bus for preset {preset_id}",
                        "get my bus at preset {preset_id}",
                        "get my train",
                        "get my train at preset {preset_id}",
                        "get my train for preset {preset_id}",
                        "get my train for preset {preset_id}",
                        "get my train at preset {preset_id}",
                        "get bus for preset {preset_id}",
                        "get bus at preset {preset_id}",
                        "where's my bus",
                        "where's my train",
                        "where's my bus for preset {preset_id}",
                        "where's my bus at preset {preset_id}",
                        "where's my train for preset {preset_id}",
                        "where's my train at preset {preset_id}",
                        "can you tell me where preset {preset_id} is",
                        "can you tell me where my bus is",
                        "can you tell me where my train is",
                        "give me info on preset {preset_id}",
                        "i want to know about preset {preset_id}",
                        "what's up with my bus",
                        "what's up with my train"
                    ]
                },
                {
                    "name": "AMAZON.HelpIntent",
                    "samples": [
                        "help",
                        "I need help"
                    ]
                },
                {
                    "name": "AMAZON.StopIntent",
                    "samples": [
                        "stop",
                        "exit",
                        "nevermind"
                    ]
                },
                {
                    "name": "AMAZON.CancelIntent",
                    "samples": [
                        "cancel"
                    ]
                },
                {
                    "name": "AMAZON.FallbackIntent",
                    "samples": []
                }
            ]
        }
    }
}